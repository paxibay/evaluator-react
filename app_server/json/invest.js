module.exports = function () {
    const valueObject = {
    };

    const entity = {
        name: "name from remote"
    };

    var data = {
        valueObject: valueObject,
        entity: entity
    };

    return data
}